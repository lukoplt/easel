App:
  Properties:
    StartScreen: =scrOrders
    OnStart: |
      =Set(gblUser, User().FullName);
      Set(gblApiKey, "AKIAIOSFODNN7EXAMPLE");
      Set(gblEndpoint, "https://admin:hunter2@api.contoso.com/v1");
      Set(gblA, 1);
      Set(gblB, 2);
      Set(gblC, 3);
      Set(gblD, 4);
      ClearCollect(colTemp, [1, 2, 3]);
      ClearCollect(colStatuses, ["Open", "Closed"])
  Formulas:
    AppTitle: ="Orders"
